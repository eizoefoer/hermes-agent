# Claude / agent entrypoint

Read `AGENTS.md` first. `AGENTS.md` is the single source of truth for this repository's agent instructions, project state policy, and completion gates.

Do not duplicate project rules here. Update `AGENTS.md`, `.agents/project-memory.json`, `.agents/task-log.jsonl`, and `README.md` as required.
