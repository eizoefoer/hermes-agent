# Agent / Model Registry

Track available workers/tools, cost tier, strengths, weaknesses, limits, allowed/disallowed tasks, and validation required before accepting output. Hermes is the orchestrator; workers do not own source-of-truth state. Every accepted worker result needs attribution in `.agents/job-ledger.jsonl`.
